import { ArrowLeft, LockKeyhole, ShieldCheck } from 'lucide-react';
import { useNavigate } from 'react-router-dom';

export default function PrivacyPage() {
  const navigate = useNavigate();
  return <div className="min-h-screen bg-slate-50 px-4 py-10 text-slate-900"><div className="mx-auto max-w-3xl"><button onClick={() => navigate(-1)} className="flex items-center gap-2 text-sm font-bold text-blue-700"><ArrowLeft className="h-4 w-4" /> Back</button><section className="mt-5 rounded-[30px] border border-slate-200 bg-white p-7 shadow-sm sm:p-10"><div className="flex items-center gap-4"><div className="rounded-2xl bg-blue-600 p-3 text-white"><LockKeyhole className="h-7 w-7" /></div><div><h1 className="text-3xl font-black text-[#1a3a6b]">Privacy & Security</h1><p className="mt-1 text-sm text-slate-500">How MedSutra protects your information.</p></div></div><p className="mt-7 text-base leading-8 text-slate-700">Your privacy is our priority. We understand that users trust us with their personal and medical information. All medical and personal data shared within the application is kept private, encrypted, and securely stored. Your information is used solely to provide healthcare-related services and improve your experience within the app. We do not sell, misuse, or share your medical information with third parties without your explicit consent, except where required by law. We are committed to maintaining the highest standards of privacy and security to ensure your data remains protected at all times.</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><SecurityCard title="Protected access" text="Password hashes, signed HttpOnly sessions, authorization checks, and automatic expiry protect account-only features." /><SecurityCard title="Secure communication" text="The production app requires HTTPS/TLS, restrictive browser policies, validated requests, and no sensitive request logging." /><SecurityCard title="Encrypted storage" text="The included server store encrypts sensitive local records using AES-256-GCM; production keys must be supplied through environment variables." /><SecurityCard title="Consent-based memory" text="Patient memory is sent to Mem0 only after explicit consent, and is used only to personalize relevant care support." /></div><div className="mt-7 rounded-2xl bg-blue-50 p-4 text-sm leading-6 text-blue-900"><ShieldCheck className="mr-2 inline h-4 w-4" />For healthcare deployments, complete a jurisdiction-specific HIPAA/GDPR and vendor compliance review before processing real patient data.</div></section></div></div>;
}
function SecurityCard({ title, text }: { title: string; text: string }) { return <div className="rounded-2xl border border-slate-100 bg-slate-50 p-5"><h2 className="font-bold text-[#1a3a6b]">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{text}</p></div>; }
